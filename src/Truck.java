public class Truck extends Vehicle {
    public Truck(String licensePlate, String color, double pricePerDay,String model, int year) {
        super(licensePlate, color, pricePerDay,model,year);;
    }
}
